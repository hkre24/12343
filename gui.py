import sys\nfrom PyQt5.QtWidgets import QApplication, QWidget, QVBoxLayout, QPushButton, QLabel, QFileDialog, QProgressBar\nimport matplotlib.pyplot as plt\nimport numpy as np\n\nclass TrainingProgress(QWidget):\n    def __init__(self):\n        super().__init__()\n        self.init_ui()\n\n    def init_ui(self):\n        self.setWindowTitle('Training Progress')\n        self.setGeometry(100, 100, 600, 400)\n        layout = QVBoxLayout()\n\n        self.upload_button = QPushButton('Upload Photo')\n        self.upload_button.clicked.connect(self.upload_file)\n        layout.addWidget(self.upload_button)\n\n        self.recognition_label = QLabel('Recognition Percentage: 0%')\n        layout.addWidget(self.recognition_label)\n\n        self.progress_bar = QProgressBar(self)\n        layout.addWidget(self.progress_bar)\n\n        self.setLayout(layout)\n\n    def upload_file(self):\n        options = QFileDialog.Options()\n        file_name, _ = QFileDialog.getOpenFileName(self, 'Choose Photo', '', 'Images (*.png; *.jpg; *.jpeg)', options=options)\n        if file_name:\n            self.start_training()\n\n    def start_training(self):\n        # Simulate training process\n        for i in range(101):\n            self.progress_bar.setValue(i)\n            self.recognition_label.setText(f'Recognition Percentage: {i}%')\n            plt.plot(np.random.rand(10), label='Training Progress')\n            plt.legend()\n            plt.pause(0.1)\n        plt.show()\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    window = TrainingProgress()\n    window.show()\n    sys.exit(app.exec_())\n